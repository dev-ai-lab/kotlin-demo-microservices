package com.shop.itemservice.repository

import com.shop.itemservice.domain.Item

val shopItems = mapOf(
    1 to Item("1", "DELL Monitor" ),
    2 to Item("2", "IPhone II" ),
    3 to Item("3", "Camera Tripod" ),
    4 to Item("4", "Alpha 7 Camera" ),
    5 to Item("5", "Camera Lens" )
)